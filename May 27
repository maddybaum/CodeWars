function nbYear(p0, percent, aug, p) {
  let year = 0;
  while (p0 < p) {
    p0 += p0 * percent/100 + aug;
    year++;
  }
  return year;
}
  
//

function isTriangle(a,b,c){
    if (a+b > c && a+c > b && c+b > a)
          return true;
        else
          return false;
}

